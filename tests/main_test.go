package tests

import (
	"testing"
)

func TestMain(m *testing.M) {
	// // Memuat variabel lingkungan dari .env sebelum menjalankan test
	// path := ".env"
	// for {
	// 	err := godotenv.Load(path)
	// 	if err == nil {
	// 		log.Println("The path is: ", path)
	// 		break
	// 	}
	// 	path = "../" + path
	// }

	// // Menjalankan semua test
	// code := m.Run()
	// os.Exit(code)
}
